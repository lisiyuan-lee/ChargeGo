# ChargeGo
电动汽车充电系统（北邮软工课设）/ EV Charging Management System（BUPT Software Engineering Curriculum Design）

//分别在两个终端中运行后端命令 `cd ChargeGo/backend && source .venv/bin/activate && python main.py` 与前端命令 `cd ChargeGo/frontend && python3 -m http.server 3000` 启动服务，随后访问 `http://127.0.0.1:3000`，使用管理员账号（`admin`/`2`）或任意用户账号（`001~020`/`1`）登录即可。
